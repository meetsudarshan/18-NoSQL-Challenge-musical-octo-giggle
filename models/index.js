// Import the User model
const User = require("./users");

// Import the Thought model
const Thought = require("./thoughts");

// Export the User and Thought models as an object
  /**
   * User Model
   *
   * The User model represents a user in the application.
   * It defines the schema and provides methods to interact with user data in the database.
   */
    /**
   * Thought Model
   *
   * The Thought model represents a thought in the application.
   * It defines the schema and provides methods to interact with thought data in the database.
   */
module.exports = { 
    User, Thought, 
};
